#ifndef GP_UTILS
#define GP_UTILS
#include <string>
using namespace std;

namespace gpUtils {
	bool arrayContains(int size, int *array, int item);
	int checkInput(string stringa, int inRange, int outRange);
}
#endif

